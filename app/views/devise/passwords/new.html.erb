<h2>Забыли пароль?</h2>

<%= semantic_form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post }) do |form| %>
  <%= devise_error_messages! %>
  <%= form.inputs do %>
    <%= form.input :email %>
  <% end %>
  <%= render :partial => "devise/shared/submit", :locals => { :form => form, :submit => :reset_password } %>
<% end %>
