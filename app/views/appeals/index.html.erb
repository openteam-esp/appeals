<%= semantic_form_for :appeal_search, :url => scoped_appeals_path(:folder => params[:folder]), :html => {:method => :get} do |form| %>
  <%= form.input :keywords %>
  <%= form.commit_button %>
<% end %>

<ul>
  <%= render @appeals %>
</ul>

<%= paginate @appeals %>

