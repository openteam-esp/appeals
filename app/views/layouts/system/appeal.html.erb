<%= content_for :header do %>
  <%= link_to t(:to_appeal_list), appeal_path(:fresh), :class => 'back_to_list' %>

  <div class='state <%= @appeal.state %>'>
    <%= I18n.t "state_machine.appeal.state.#{@appeal.state}" %>
  </div>

  <div class="composed_title">
    <%= @appeal.full_name %>
  </div>
<% end  %>

<%= content_for :content do %>
  <%= yield %>
<% end %>

<%= render :template => 'layouts/layer' %>
