<%= content_for :header do %>
  <%= link_to t(:to_appeal_list), manage_scoped_appeals_path(:folder => @appeal.state), :class => 'back_to_list' %>

  <div class='state <%= @appeal.state %>'>
    <%= @appeal.human_state_name %>
  </div>

  <div class="composed_title">
    <%= @appeal.author %>
  </div>
<% end  %>

<%= content_for :content do %>
  <%= yield %>
<% end %>

<%= render :template => 'layouts/layer' %>
