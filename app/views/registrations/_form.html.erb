<%= semantic_form_for @appeal.build_registration, :url => appeal_registration_path(@appeal) do |form| %>
  <%= form.inputs :registered_on, :number, :directed_to %>
  <%= form.commit_button %>
<% end %>

