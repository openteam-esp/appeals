<%= link_to t('note'), '#', :class => 'button icon approve', :id => 'note' %>
<%= semantic_form_for @appeal.build_note, :url => manage_appeal_note_path(@appeal), :validate => true do |form| %>
  <%= form.inputs :public if @appeal.public %>
  <%= form.actions do %>
    <%= form.action :submit, :label => t('send'), :button_html => { :class => "button note" } %>
  <% end %>
<% end %>
