<%= semantic_form_for @appeal.reply || @appeal.build_reply, :url => appeal_reply_path(@appeal)  do |form| %>
  <%= form.inputs :number, :replied_on, :replied_by, :text, :public %>
  <%= form.buttons do %>
    <%= form.commit_button %>
  <% end %>
<% end %>
